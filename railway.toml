# Railway Configuration for RAPTOR
# Service-specific configs in apps/*/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[build.buildArgs]
DOCKER_BUILDKIT = "1"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
